<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.garganttua</groupId>
        <artifactId>garganttua-events-examples</artifactId>
        <version>3.0.0-ALPHA04</version>
    </parent>

    <artifactId>garganttua-events-example-bus</artifactId>
    <description>Minimal runnable garganttua-events demo: defines an events topology with
        the EventsBuilder DSL, lets the Bootstrap auto-wire reflection / injection /
        expression, and retrieves the built IEvents engine (the in-memory bus connector
        module is on the classpath so the 'bus' connector type is available).</description>

    <properties>
        <exec.mainClass>com.garganttua.events.example.EventsDemo</exec.mainClass>
    </properties>

    <dependencies>
        <!-- The events DSL + engine. -->
        <dependency>
            <groupId>com.garganttua</groupId>
            <artifactId>garganttua-events-core</artifactId>
            <version>${garganttua.version}</version>
        </dependency>
        <!-- In-memory bus connector (registers the 'bus' connector type). -->
        <dependency>
            <groupId>com.garganttua</groupId>
            <artifactId>garganttua-events-connector-bus</artifactId>
            <version>${garganttua.version}</version>
        </dependency>
        <!-- Bootstrap: wires reflection + injection + expression + the auto-loaded
             events module together with zero manual plumbing. -->
        <dependency>
            <groupId>com.garganttua.core</groupId>
            <artifactId>garganttua-bootstrap</artifactId>
            <version>${garganttua.version}</version>
        </dependency>
        <!-- Cold-start reflection provider + annotation scanner (JVM mode). -->
        <dependency>
            <groupId>com.garganttua.core</groupId>
            <artifactId>garganttua-runtime-reflection</artifactId>
            <version>${garganttua.version}</version>
        </dependency>
        <dependency>
            <groupId>com.garganttua.core</groupId>
            <artifactId>garganttua-reflections</artifactId>
            <version>${garganttua.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- mvn -f events/examples/pom.xml -pl example-bus exec:java -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec.plugin.version}</version>
                <configuration>
                    <mainClass>${exec.mainClass}</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
